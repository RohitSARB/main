# Understanding OAuth in Microservices

OAuth is an authorization framework that allows services to access resources on behalf of users.

## OAuth Flow

The OAuth flow consists of:
1. Authorization request
2. Authorization grant
3. Access token exchange

In microservices, OAuth is often used with API gateways.

## Common Issues

- Token expiration
- Refresh token mismanagement
- Incorrect scopes
